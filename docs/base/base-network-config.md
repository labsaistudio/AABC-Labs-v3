# Base Network Config

The Base-only network config lives in:

```text
src/base/network-config.mjs
```

It declares:

- Base mainnet: chain id `8453`, RPC `https://mainnet.base.org`
- Base Sepolia: chain id `84532`, RPC `https://sepolia.base.org`

The current deployment target is Base Sepolia until the proof path has live
review and funding evidence.
