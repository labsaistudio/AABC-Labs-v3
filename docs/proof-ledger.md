# Proof Ledger

The proof ledger records:

- prepared transaction records
- generated artifacts
- source package manifests
- public proof feeds

Public feeds are redacted before export. Private payload fields are never
required for local demos.
